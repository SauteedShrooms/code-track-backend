<%= form_with(model: spoiler) do |form| %>
  <% if spoiler.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(spoiler.errors.count, "error") %> prohibited this spoiler from being saved:</h2>

      <ul>
        <% spoiler.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :speed %>
    <%= form.number_field :speed %>
  </div>

  <div class="field">
    <%= form.label :efficency %>
    <%= form.number_field :efficency %>
  </div>

  <div class="field">
    <%= form.label :power %>
    <%= form.number_field :power %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
